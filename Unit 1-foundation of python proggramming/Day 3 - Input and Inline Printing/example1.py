age = 40
name = "ishaan"

print("name: " + name + ". Age: " + str(age))
#works but we need + everywhere
#we have to change everything into a string using str()
# the other option is to use f-strings ( formatted strings)

print(f"name:  {name}. Age: {age} years.")
